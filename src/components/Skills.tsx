import { myInfo } from "@/intro";
import React from "react";

const Experience = () => {
	return (
		<section className="py-10 md:py-16 w-full">{/* {myInfo?.} */}</section>
	);
};

export default Experience;
